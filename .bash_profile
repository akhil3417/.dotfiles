#
# ~/.bash_profile fs
#
[[ -f ~/.bashrc ]] && . ~/.bashrc
# gpg has to be started here (or in .xinitrc.exwm), if we want to have encryption in exwm
export GITHUB_TOKEN="[TOKEN HERE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]"
